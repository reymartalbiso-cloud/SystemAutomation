# Local dev uses SQLite (single file, zero setup)
DATABASE_URL="file:./dev.db"

# Session signing secret (use a long random string in production)
AUTH_SECRET="change-me-to-a-long-random-string-in-production"

# --- For Vercel deployment ---
# Swap to Postgres (Vercel Postgres, Neon, or Supabase) for persistent storage
# DATABASE_URL="postgresql://user:password@host:5432/commission_tracker?sslmode=require"
